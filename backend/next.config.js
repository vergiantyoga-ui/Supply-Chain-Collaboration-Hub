/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // This Next.js app is used purely as an API backend (App Router route handlers).
  // The React/Vite frontend lives in ../frontend and calls these endpoints over HTTP.
};

module.exports = nextConfig;
